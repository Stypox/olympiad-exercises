#include <bits/stdc++.h>

using namespace std;

void scriviOutput(vector<int> sol) {
	for(int i = 0; i < sol.size(); i++) {
		cout << sol[i] << "\n";
	}
}

int main() {
	vector<int> sol;
	scriviOutput(sol);
}
